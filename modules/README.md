# Modules

Domain modules own business-specific UI, hooks, API adapters, schemas, types, utilities, stores, pages, and actions.

The App Router should import from modules when a route needs domain behavior, but route files should stay thin and routing-focused.
